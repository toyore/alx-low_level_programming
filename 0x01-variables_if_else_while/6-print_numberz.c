#include <stdio.h>
/**
 * main - main function
 * Description: This is a program that prints digit in base 10
 * Return: (0) success
 */
int main(void)
{
	int digit;
	for (digit = 0; digit <= 9; digit++)
	{
		putchar(digit);
		putchar('\n');
	}
return (0);
}
