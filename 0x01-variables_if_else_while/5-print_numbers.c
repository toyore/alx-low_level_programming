#include <stdio.h>
/**
 * main - main function
 * Description: This is a program that prints digits if base 10
 * Return: allows (0) success
 */
int main(void)
{
	for (int digit = 0; digit <= 9; digit++)
	{
		printf("%d/n", digit);
	}
return (0);
}

